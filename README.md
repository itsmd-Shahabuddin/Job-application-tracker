1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
Ans:
getElementById = one element by id
getElementsByClassName = multiple elements
querySelector =first match
querySelectorAll =all matches

2. How do you create and insert a new element into the DOM?
Ans:
document.createElement()=create element
appendChild()= add to DOM

3. What is Event Bubbling? And how does it work?
Ans:Event bubbling = event goes child and than parent

4. What is Event Delegation in JavaScript? Why is it useful?
Ans:
Event delegation = one parent event handles child events
and this is Useful for dynamic elements

5. What is the difference between preventDefault() and stopPropagation() methods?
Ans:
preventDefault()= stops default action
stopPropagation() = stops event bubbling
